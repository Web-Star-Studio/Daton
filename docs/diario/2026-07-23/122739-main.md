---
hora: "12:27"
autor: João Pedro
branch: main
modulo: Aprendizagem
titulo: Evidência obrigatória ao registrar requisito — mergeado (PR #200)
---

## Aprendizagem — Evidência obrigatória ao registrar requisito de competência: MERGEADO (PR #200)

**Atualização do registro anterior:** o PR #200 (campo "Evidência" obrigatório no diálogo "Registrar evidência" da ficha do colaborador) foi revisado, testado e **mergeado na main** (squash `feacd0bb`, branch remoto removido). Estava registrado como "aberto em draft" no fragmento anterior do mesmo dia — este fragmento atualiza o status final.

**Validação adicional feita antes do merge:** subida local (ambiente isolado, banco descartável, sem tocar dado de produção/Gabardo) para navegação manual na ficha de um colaborador com requisito pendente — confirmado visualmente que (1) o botão "Salvar" fica desabilitado com a evidência vazia, mostrando "Evidência é obrigatória"; (2) preenchendo somente o texto (sem anexo) o botão habilita e o requisito passa a "Atende" imediatamente. O usuário validou o mesmo fluxo pelo próprio navegador antes de autorizar o merge.

**Decisão de produto confirmada com o usuário:** o texto de evidência sozinho (sem anexo) já é suficiente para validar o requisito — o anexo permanece opcional, por escolha explícita. Tecnicamente isso já era assim: o resolvedor de status ("atende"/"gap") nunca considerou `evidence`/`attachments`, só o nível adquirido — a mudança deste PR apenas impede salvar o registro de evidência sem descrever o texto.

**Pendência conhecida, fora do escopo deste PR:** o fluxo "Outras competências" (cadastro manual fora do requisito do cargo) ainda permite salvar sem evidência — não foi alterado agora.

**Status:** Mergeado. Branch de trabalho e recursos locais de teste (banco docker temporário, servidores de dev) foram removidos ao final.
